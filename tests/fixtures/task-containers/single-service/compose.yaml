services:
  web:
    image: busybox:1.36.1
    command:
      - sh
      - -c
      - mkdir -p /www && echo ok >/www/index.html && echo task-container-ready && httpd -f -p 5678 -h /www
    ports:
      - "127.0.0.1::5678"
